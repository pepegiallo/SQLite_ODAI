#int {
    INTEGER
}
#text100 {
    VARCHAR(100)
}
#text {
    TEXT
}
#date {
    DATE,
    get {
        value = parse_sqlite_date(value)
    }
}
#array {
    BLOB,
    get {
        value = bytes_to_array(value)
    },
    set {
        value = array_to_bytes(value)
    }
}
+attributes {
    first_name: text100,
    last_name: text100,
    number: int,
    street: text100,
    house_number: text100,
    zip: text100,
    city: text100,
    birthday: date,
    entry_date: date,
    some_numbers: array
}
Address {
    street,
    house_number,
    zip,
    city
}
Person {
    first_name,
    last_name,
    birthday,
    ~person_to_address -> Address
}
Employee(Person) {
    number*,
    entry_date,
    ~manager_to_employees -> Employee
}
ArrayData {
    some_numbers
}